/* firebase-config.js — fill this in ONCE with your Firebase web config, then commit & push.
   It is NOT a secret (the web config is public in every Firebase app), so it's safe to put here.
   Once filled in, everyone who opens the app gets team sync automatically — they only sign in with
   Google once. Your data stays protected by the Firestore security rules (the email allowlist).

   Where to get these: Firebase console → Project settings (gear) → General → Your apps → the Web app
   → "SDK setup and configuration" → Config. Copy each value into the matching field below.

   Leave it as-is (empty projectId) to keep team sync OFF / manual-entry only. */
window.LABOSPORT_FB_CONFIG = {
  apiKey: "AIzaSyCfMTIj2W3YexnYJfDqaMhdR4qm8v-WnY4",
  authDomain: "labosport-app.firebaseapp.com",
  projectId: "labosport-app",
  storageBucket: "labosport-app.firebasestorage.app",
  messagingSenderId: "667641591422",
  appId: "1:667641591422:web:74116b92c089222b8b4a2f",
  measurementId: "G-N5GMGD111B"
};
